### Copy this file to `.env.local` for local development.
### Production environments should continue to supply `DATABASE_URL`.

# Primary Prisma datasource (also used by CLI commands when running locally)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/household?schema=public"

# Optional runtime override used when `DATABASE_URL` is not set (dev-only)
LOCAL_DATABASE_URL="postgresql://postgres:postgres@localhost:5433/household?schema=public"

# Optional: point the UI at an existing household record (useful when restoring prod DB locally)
# HOUSEHOLD_ID="your-household-id"

